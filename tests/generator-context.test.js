'use strict'; const definition=require('../examples/ai_business_model_departments.json'); const {createGeneratorContext}=require('../src/core/generator-context');
test('detecta capacidades SBN',()=>{const c=createGeneratorContext(definition,{generatedAt:'2026-07-18T00:00:00.000Z'});expect(c.capabilities).toMatchObject({softDelete:true,audit:true,versioning:true,json:true,foreignKeys:true});expect(c.writableColumns.map(x=>x.name)).toEqual(['business_model_id','department_id','is_required','execution_order','recommended_plan','configuration_json']);expect(c.permissions).toContain('AI_BUSINESS_MODEL_DEPARTMENTS_VIEW');});
